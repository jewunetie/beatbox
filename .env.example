# Spotify Web API credentials (Client Credentials flow).
# Get them at https://developer.spotify.com/dashboard
# The app owner must have Spotify Premium.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Used by Prisma (loaded via prisma.config.ts → dotenv).
DATABASE_URL="file:./dev.db"
